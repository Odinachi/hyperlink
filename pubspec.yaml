name: hyperlink
version: 1.0.0
description: A Flutter package for rendering text with clickable hyperlinks.
homepage:

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0



